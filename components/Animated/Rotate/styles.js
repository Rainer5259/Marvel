import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  arrow: {
    alignItems: 'center',
    justifyContent: 'center',
    position: 'absolute',
    right: '-6%',
    width: 24,
    height: 24,
  },
});
