import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  thumbnail: {
    top: '2%',
    height: 150,
    resizeMode: 'stretch',
    borderRadius: 10,
    marginBottom: '10%',
    shadowOpacity: 10,
  },
});
export default styles;
