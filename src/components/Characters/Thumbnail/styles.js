import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  thumbnail: {
    top: '2%',
    height: 320,
    resizeMode: 'stretch',
    borderRadius: 12,
    marginBottom: '10%',
  },
});
export default styles;
